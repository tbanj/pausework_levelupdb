{
  "name": "levelup_db",
  "version": "1.0.0",
  "description": "practice how to link database through npm knex.js to node project which is power by express",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-migration": "./node_modules/.bin/knex migrate:make --env development",
    "create-seed": "./node_modules/.bin/knex seed:make --env development",
    "seed": "./node_modules/.bin/knex seed:run",
    "migrate:up": "./node_modules/.bin/knex migrate:latest --env development",
    "migrate:down": "./node_modules/.bin/knex migrate:rollback --env development"
  },
  "author": "Alabi Temitope Wahab",
  "license": "ISC",
  "dependencies": {
    "bookshelf": "^0.14.2",
    "dotenv": "^7.0.0",
    "dotenv-expand": "^5.1.0",
    "dotenv-extended": "^2.4.0",
    "dotenv-parse-variables": "^0.2.0",
    "express": "^4.16.4",
    "faker": "^4.1.0",
    "knex": "^0.16.3",
    "moment": "^2.24.0",
    "mysql": "^2.16.0"
  }
}
